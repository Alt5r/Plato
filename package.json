{
  "name": "secureskills",
  "description": "Secure skills for coding agents with local authorization and verified runtime exposure.",
  "type": "module",
  "bin": {
    "secureskills": "./bin/secureskills.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "node ./bin/secureskills.js",
    "test": "node --experimental-strip-types --test tests/*.test.ts"
  }
}
